{# Blogger:AdminBundle:Admin:roles.html.twig namespace #}

{% extends "AdminBundle::base.html.twig" %}

{% block content %}
    <p>
        <a href="{{ path("admin") }}">Назад</a>
    </p>
    <hr>
    <a href="{{ path("admin_roles_create") }}">
        <div>+</div>
    </a>
    {% for role in roles %}        
        <a href="{{ path("role", {id: role.id}) }}">
            {{ role.name }}
            {{ role.role }}
        </a>
        <hr>
    {% endfor %}
{% endblock %}

{% block admin_side %}
    Roles side
{% endblock %}
